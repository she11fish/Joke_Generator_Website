<!DOCTYPE html>
<head>
    <body>
        <link rel="preconnect" href="https://fonts.googleapis.com">
        <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
        <link href="https://fonts.googleapis.com/css2?family=New+Rocker&display=swap" rel="stylesheet">
        <link rel="preconnect" href="https://fonts.googleapis.com">
        <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
        <link href="https://fonts.googleapis.com/css2?family=Cabin&display=swap" rel="stylesheet">
        <link rel="preconnect" href="https://fonts.googleapis.com">
        <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
        <link href="https://fonts.googleapis.com/css2?family=Roboto&display=swap" rel="stylesheet">
        <div class="box">
            <nav>
                <a class="home" href="/">HOME</a>
                <a class="joke" href="/login">JOKE</a>
                <% if (!delete_button) { %>
                <a class="login" href="/login">LOG IN</a>
                <a class="sign_up" href="http://localhost:5500/views/create_user.html">SIGN UP</a>
                <% } %>
                <% if (delete_button) { %>

                    <div class="delete_account"> 
                        <button type="submit" class="delete_button" onclick="response()">DELETE ACCOUNT</button>
                    </div>
                    </form>
                    <form action="/logout?_method=DELETE" method="POST">
                        <input type="hidden">
                        <button class="logout">LOG OUT</button>
                    </form>
                    
                <% }%>
            </nav>
        </div>
        <div>
            <img src="/images/Trollface.png" id="troll_face" width="700" height="700">
        </div>
        <h2>The best comedic website ever</h2>
        <div> 
            <p id="paragraph">You can definetly trust us. There's nothing suspicious. Totally unSUSpicious!!!</p> 
        </div>
        <audio src="/mp3/Scheming Weasel Faster (Kevin Macleod) - Vanoss Gaming Background Music (HD) (320 kbps).mp3" autoplay>
            <p>If you are reading this, it is because your browser does not support the audio element.</p>
        </audio>
        <div class="container">
            <button id="close_button" type="submit" onclick="close_window()">&times;</button>
            <div class="test">
                <p id="test">Are you sure you want to delete your account?</p>
                <form action="/delete?_method=DELETE" method="POST">
                    <button id="yes" type="submit">YES</button>
                </form>
                <button id="no" type="submit" onclick="close_window()">NO</button>
            </div>
        </div>
        <link rel="stylesheet" href="/css/index.css" type="text/css" />
        <script>
            function response() 
            {
                document.getElementsByClassName('container')[0].style.display = "block";
                document.getElementsByTagName('html')[0].style.position = "fixed";
            }
            function close_window()
            {
                document.getElementsByClassName('container')[0].style.display = "none";
                document.getElementsByTagName('html')[0].style.position = "relative";
            }
        </script>
    </body>
</head>